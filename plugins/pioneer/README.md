# Pioneer plugin

This directory is the shared plugin payload for Codex and Claude Code. Both manifests load the same `pioneer` skill; the plugin does not bundle provider credentials or the Pioneer executable.

Install the Pioneer CLI and configure Pi before installing the plugin. See [`user-guide/plugins.md`](../../user-guide/plugins.md) for installation and validation instructions.
